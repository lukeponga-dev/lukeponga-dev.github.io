const themeToggle = document.getElementById("themeToggle"), moonIcon = document.getElementById("moonIcon"), sunIcon = document.getElementById("sunIcon"), body = document.body, currentTheme = localStorage.getItem("theme") || "dark"; "light" === currentTheme && (body.classList.add("light-mode"), moonIcon?.classList.add("hidden"), sunIcon?.classList.remove("hidden")), themeToggle?.addEventListener("click", () => { body.classList.toggle("light-mode"); const e = body.classList.contains("light-mode"); localStorage.setItem("theme", e ? "light" : "dark"), moonIcon?.classList.toggle("hidden", e), sunIcon?.classList.toggle("hidden", !e), document.getElementById("heroCanvas") && setCanvasSize() }); const mobileMenuButton = document.getElementById("mobileMenuButton"), mobileMenu = document.getElementById("mobileMenu"), menuIcon = document.getElementById("menuIcon"), closeIcon = document.getElementById("closeIcon"), mobileLinks = document.querySelectorAll(".mobile-nav-link"); function toggleMobileMenu() { mobileMenu.classList.toggle("hidden"); const e = mobileMenu.classList.contains("hidden") ? "false" : "true"; mobileMenuButton.setAttribute("aria-expanded", e), menuIcon?.classList.toggle("hidden"), closeIcon?.classList.toggle("hidden") } mobileMenuButton?.addEventListener("click", toggleMobileMenu), mobileLinks.forEach(e => e.addEventListener("click", () => { mobileMenu.classList.contains("hidden") || toggleMobileMenu() })); const header = document.querySelector("header"), scrollThreshold = 50; function handleScroll() { header && (window.scrollY > scrollThreshold ? (header.classList.add("bg-slate-900", "shadow-xl", "light-mode:bg-white", "scrolled"), header.classList.remove("bg-slate-900/90", "light-mode:bg-white/90")) : (header.classList.remove("bg-slate-900", "shadow-xl", "light-mode:bg-white", "scrolled"), header.classList.add("bg-slate-900/90", "light-mode:bg-white/90"))) } document.addEventListener("scroll", handleScroll), handleScroll(); let canvas, ctx, particles = [], particleCount = 50; function setCanvasSize() { if (canvas) { const e = canvas.parentElement; canvas.width = e.clientWidth, canvas.height = e.clientHeight, 0 === particles.length && function () { for (let e = 0; e < particleCount; e++)particles.push(new Particle) }() } } class Particle { constructor() { this.x = Math.random() * canvas.width, this.y = Math.random() * canvas.height, this.vx = .5 * (Math.random() - .5), this.vy = .5 * (Math.random() - .5), this.radius = 2.5 * Math.random() + .5 } update() { this.x += this.vx, this.y += this.vy, (this.x < 0 || this.x > canvas.width) && (this.vx *= -1), (this.y < 0 || this.y > canvas.height) && (this.vy *= -1) } draw() { ctx.beginPath(), ctx.arc(this.x, this.y, this.radius, 0, 2 * Math.PI), ctx.fillStyle = body.classList.contains("light-mode") ? "rgba(59,130,246,0.2)" : "rgba(59,130,246,0.5)", ctx.fill() } } function animate() { ctx.fillStyle = body.classList.contains("light-mode") ? "rgba(248,250,252,0.3)" : "rgba(12,18,30,0.3)", ctx.fillRect(0, 0, canvas.width, canvas.height), particles.forEach(e => { e.update(), e.draw() }), requestAnimationFrame(animate) } window.onload = () => { (canvas = document.getElementById("heroCanvas")) && (ctx = canvas.getContext("2d"), setCanvasSize(), window.addEventListener("resize", setCanvasSize), animate()) }; function setLayout(e) { const t = document.getElementById("grid-layout"), a = document.getElementById("list-layout"), n = document.getElementById("grid-view-btn"), d = document.getElementById("list-view-btn"); "grid" === e ? (t.classList.remove("hidden"), a.classList.add("hidden"), n.classList.add("bg-cyan-600"), n.classList.remove("bg-slate-700"), d.classList.remove("bg-cyan-600"), d.classList.add("bg-slate-700")) : (t.classList.add("hidden"), a.classList.remove("hidden"), d.classList.add("bg-cyan-600"), d.classList.remove("bg-slate-700"), n.classList.remove("bg-cyan-600"), n.classList.add("bg-slate-700")) } document.addEventListener("DOMContentLoaded", () => { setLayout("grid"), document.getElementById("grid-view-btn")?.addEventListener("click", () => setLayout("grid")), document.getElementById("list-view-btn")?.addEventListener("click", () => setLayout("list")), document.querySelectorAll(".project-card").forEach((e, t) => e.style.animationDelay = `${.1 * t}s`) }), document.querySelectorAll('a[href^="#"]').forEach(e => { e.addEventListener("click", function (t) { const a = this.getAttribute("href"); a.length > 1 && (t.preventDefault(), document.querySelector(a)?.scrollIntoView({ behavior: "smooth" })) }) });